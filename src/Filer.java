import javax.swing.*;
import java.io.*;
import java.util.Scanner;

public class Filer
{

    /*public static String getIndhold (String sti)  //metode til at læse en fil
    {
        String indhold = "";   //sætter 'indhold' til en tom String
        try
        {
            Scanner sc = new Scanner(new File(sti));  //læser en fil

            indhold += sc.nextLine();
            while(sc.hasNextLine())  //læser filen linje for linje så længe det indeholder linjer
            {
                indhold += '\n' + sc.nextLine();  //opdaterer 'indhold' med de linjer, der læses
            }
        }
        catch(FileNotFoundException e)  //fortæller hvad der skal ske, hvis den valgte fil ikke findes - eller der er risiko for at filen ikke findes
        {
            System.out.println("Filen eksisterer ikke");  //fejlbesked
        }

        return indhold;  //returnerer i den fil, der er læst
    }

    public static void skrivFil (String sti, String indhold)  //skriver en fil
    {
        try
        {
            PrintStream output = new PrintStream(new File(sti));  //PrintStream objekt som sender output til en fil
            output.print(indhold);
        }
        catch(FileNotFoundException e)  //fortæller hvad der skal ske, hvis den valgte fil ikke findes - eller der er risiko for at filen ikke findes
        {
            System.out.println("Kan ikke tilgå fil: " + sti);  //fejlbesked
        }

    }*/



}



